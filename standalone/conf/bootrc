# Additional init commands, run at the beginning of flywheel-launch.sh, prior to
# launching the broker JVM process.

export JVM_OPTS="-XX:-MaxFDLimit -XX:+TieredCompilation -XX:+UseNUMA -XX:+UseCondCardMark -XX:-UseBiasedLocking -Xms1G -Xmx2G -Xss1M -XX:MaxDirectMemorySize=2G -XX:+UseParallelGC"